name: CodeQL

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    # Weekly Monday 06:00 UTC
    - cron: '0 6 * * 1'

permissions:
  contents: read
  actions: read
  security-events: write

jobs:
  analyze:
    name: Analyze
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v3 # TODO(security): pin to commit SHA
        with:
          # `javascript` scans the small number of real .mjs/.js scripts in
          # scripts/ and plugins/; `actions` scans the workflow/Action files.
          languages: javascript, actions

      - name: Autobuild
        uses: github/codeql-action/autobuild@v3 # TODO(security): pin to commit SHA

      - name: Analyze
        uses: github/codeql-action/analyze@v3 # TODO(security): pin to commit SHA
