## Plan

### Part A — Commit the uncommitted work on `feat/delegate-my-work-skill` (two commits)

**Step 1: Bump everything from 3.3.x → 3.4.0**
- Set every plugin's `plugin.json` to `"version": "3.4.0"` (covers the 170 files that are currently at 3.3.0 plus any that drifted) and update the 171 entries in `.claude-plugin/marketplace.json` to 3.4.0.
- Update `README.md`: change `Version 3.3.1` → `3.4.0`, update the "Last Updated" date to 2026-07-18, and update the skill count line (will be 140 after the new plugin lands — see Step 3; I'll set it once at the end so it's correct).
- Add a new `## [3.4.0] - 2026-07-18` section at the top of `CHANGELOG.md` with Added/Changed entries describing: (a) the `delegate-my-work` skill upgrade, (b) the new `typescript-migration` plugin, (c) the global 3.3.x → 3.4.0 version bump. (The cleanup branch's own 3.4.0 entry gets reconciled at merge time in Part B.)
- Commit message: `chore: bump marketplace and all plugins to v3.4.0`

**Step 2: Add the new `typescript-migration` plugin (separate commit)**
- `git add plugins/typescript-migration/` (its `marketplace.json` entry is already present from the uncommitted work; this just tracks the directory so the manifest reference resolves).
- Verify the entry sits between `typescript-mcp` and `ultracite` in alphabetical order (it does).
- Commit message: `feat(typescript-migration): add TypeScript 5/6/7 migration plugin`

Note on the `delegate-my-work` metadata change: its description/keyword tweak is already part of the working tree and will ride along with the version bump commit (the file is touched by both). That's acceptable — the change is small and is exactly what this branch is about.

### Part B — Merge both branches to `main`

**Step 3: Merge `chore/cleanup-deprecated-plugins-commands` FIRST (fast-forward or no-ff)**
- This branch removes 31 deprecated plugins, deletes `commands/`, and already contains a 3.4.0 CHANGELOG/README. Merging it first establishes the correct v3.4.0 baseline and the 139-plugin set.
- Expected: clean merge (it diverged directly from main).
- This also closes the manifest references to the removed plugins.

**Step 4: Merge `feat/delegate-my-work-skill` on top**
- This branch adds `delegate-my-work`, `typescript-migration`, the validator script + pre-commit hook, and the v3.4.0 bumps.
- Expected conflicts (all mechanical):
  - `CHANGELOG.md` — both branches have a `## [3.4.0]` section. I'll merge them into one combined 3.4.0 entry on main.
  - `README.md` — both branches touch the version/skill-count line. I'll resolve to `Version 3.4.0`, today's date, and a final skill count of **140** (139 from cleanup + 1 for `typescript-migration`; `delegate-my-work` was already in the count before).
  - `marketplace.json` — cleanup deleted 31 entries; this branch adds 1 (`typescript-migration`) and bumps versions. Both edits apply cleanly to disjoint regions; I'll take the union.
  - Any `commands/` removal overlap — trivial, both sides delete the same files.
- I'll verify the final `marketplace.json` parses as valid JSON and that every entry's `source` directory exists before finalizing.

### Part C — Push
- Push the two new commits on `feat/delegate-my-work-skill` to its remote.
- Push `main` (both merges) to origin.
- PR #65 will auto-close when the cleanup branch merges to main (I'll reference it in the merge commit message).

### Things I will NOT do
- Will not force-push or rewrite history on `main`.
- Will not delete any local feature branches.
- Will not touch the 27 already-merged branches.

If anything unexpected surfaces during the merges (e.g. a conflict I didn't predict), I'll stop and report before improvising.